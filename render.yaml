services:
  - type: web
    name: skin-lesion-segment
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libgl1
      pip install -r requirements.txt
    startCommand: gunicorn app:app
